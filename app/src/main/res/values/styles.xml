<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="customSeekbar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:minHeight">2dp</item>
        <item name="android:maxHeight">2dp</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="videoStartSeekBar" parent="customSeekbar">
        <item name="android:progressDrawable">@drawable/seekbar_video_start_background</item>
        <item name="android:thumb">@drawable/shape_seekbar_controller</item>
    </style>

    <style name="videoDurationSeekBar" parent="customSeekbar">
        <item name="android:progressDrawable">@drawable/seekbar_video_duration_background</item>
        <item name="android:thumb">@drawable/shape_seekbar_controller</item>
    </style>

</resources>
